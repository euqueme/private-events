<h1>Log in</h1>
<div>
  <div>
    <%= form_for(:session, url: login_path) do |f| %>

      <%= f.label :id %>
      <%= f.text_field :ide %>


      <%= f.submit "Log in"%>

    <% end %>

    <p>New user? <%= link_to "Sign up now!", signup_path %></p>
  </div>
</div>
